<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm" href="/logout">Logout</a>
        </div>
        
        <h3 class="text-3xl mt-5">Hello, <%= user.name%> 👋🏻</h3>
        <h5 class="mb-4">You can create a new post!</h5>
        <form method="post" action="/post">
            <textarea placeholder="What's on your mind?" class="block p-2 w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md outline-none" name="content" id="content"></textarea>
            <input class="block px-3 py-2 bg-blue-500 rounded-md mt-2 w-40 text-sm" type="submit" value="Create New Post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your posts.</h3>
            <div class="postcontainer mt-4">
                 <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 mb-2 p-3 border-[1px] bg-zinc-800 border-zinc-600 rounded-md">
                    <h4 class="text-blue-500 mb-4">@<%= user.username %></h4>
                    <p class="text-sm tracking-tight"><%= post.content %></p>
                    <small class="mt-3 inline block"><%= post.likes.length%> likes</small>
                    <div class="btns flex mt-1  gap-4">
                        <a class="text-blue-500" href="/like/<%= post._id%>">
                            <%= post.likes.indexOf(user.id) === -1 ? "like" : "Unlike" %>
                        </a>
                        <a class="text-zinc-600" href="/edit/<%= post._id %>">Edit</a>
                    </div>
                </div>
               <% }) %>
            </div>
        </div>
    </div>
</body>
</html>